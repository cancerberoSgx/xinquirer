{
  "name": "xinquirer",
  "version": "1.0.0",
  "description": "like inquierer but for X - dialogs that pop up on top of user's window to ask for information",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp -rf src/assets dist",
    "start": "rm -rf dist/ && npm run build && electron dist/index.js"
  },
  "keywords": [
    "inquierer",
    "electron",
    "data",
    "input",
    "UI",
    "X",
    "filechooser"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^2.0.0"
  },
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "@types/node": "^10.0.8",
    "typescript": "^2.8.3"
  }
}
